import React from "react";

const Servicios = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
        fontSize: "3rem",
      }}
    >
      Servicios
    </div>
  );
};

export default Servicios;
